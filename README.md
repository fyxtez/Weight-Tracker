# Weight Tracker full app

```text
app/      React + Tauri application for Android and desktop
backend/  Rust + Axum synchronization API
```

## Run the backend

See [`backend/README.md`](backend/README.md) for PostgreSQL startup and curl smoke tests.

## Run the app

```bash
cd app
npm install
npm run tauri dev
```
